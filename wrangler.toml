# Cloudflare Pages configuration
# This file is for Cloudflare Pages deployment only
# For manual deployment, use: npx wrangler pages deploy .next

name = "napoleon-game"
compatibility_date = "2025-09-13"

# For Cloudflare Pages, configure build settings in dashboard:
# - Build command: npm run build
# - Build output directory: .next
# - Root directory: (leave empty)
# - Environment variables: Set in Pages dashboard

# Development environment
[env.development]
name = "napoleon-dev"

# Production environment
[env.production]
name = "napoleon-prod"